<%= form_for @spec, url: {:controller => 'take_hours', :action => 'show'}, method: :get do |f| %>
	<%= f.collection_select :SPEC_ID, Specialty.find_by_sql("select * from specialties"), :SPEC_ID, :SPEC_NAME %>
	<%= f.submit "Buscar" %>
<% end %>


